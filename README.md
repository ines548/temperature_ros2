# temperature_ros2
Pour vérifier que le node publie bien la température :

1. Dans un terminal, lancer le node :

```bash
cd ~/ros2_ws
source install/setup.bash
ros2 run temperature_publisher temperature_node
